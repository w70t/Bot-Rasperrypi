🔥 ممتاز جدًا عبدالوهاب 👏👏
واضح إنك وصلت لمرحلة الاحتراف ما شاء الله —
الآن راح أجهز لك كل الأوامر المهمة واللي تحتاجها لإدارة البوت على الـ Raspberry Pi
كأنك تملك سيرفر فعلي. 💪

راح أقسمها إلى فصول مرتّبة 👇

🧱 أولاً: معلومات الخدمة (botiraq)

اسم الخدمة اللي أنشأناها هو:

botiraq.service


⸻

⚙️ ثانياً: أوامر التشغيل والتحكم

الهدف	الأمر
🔹 تشغيل البوت يدويًا	sudo systemctl start botiraq
🔹 إيقاف البوت	sudo systemctl stop botiraq
🔹 إعادة تشغيل البوت	sudo systemctl restart botiraq
🔹 التحقق من حالته	sudo systemctl status botiraq
🔹 جعله يبدأ تلقائيًا عند التشغيل	sudo systemctl enable botiraq
🔹 تعطيل التشغيل التلقائي	sudo systemctl disable botiraq


⸻

🧾 ثالثاً: مراقبة السجلات (Logs)

لمشاهدة السجلات لحظة بلحظة (تفاعلات + أخطاء):

sudo journalctl -u botiraq -f

للخروج من عرض السجلات اضغط:
Ctrl + C

لمشاهدة آخر 100 سطر فقط:

sudo journalctl -u botiraq -n 100


⸻

⚙️ رابعاً: تعديل ملف البيئة .env

هذا الملف يحتوي بيانات حساسة مثل التوكنات.
مساره داخل مجلد المشروع:

/home/abdalwahab/Bot-iraq/.env

لتعديله:

nano /home/abdalwahab/Bot-iraq/.env

بعد التعديل، احفظ بـ:

Ctrl + O
Enter
Ctrl + X

ثم أعد تشغيل البوت ليقرأ الإعدادات الجديدة:

sudo systemctl restart botiraq


⸻

🧰 خامساً: تحديث المشروع من GitHub

1️⃣ انتقل إلى مجلد البوت:

cd /home/abdalwahab/Bot-iraq

2️⃣ تحقق من التغييرات الجديدة على GitHub:

git pull

إذا ظهرت رسالة تقول “Already up to date.”
يعني المشروع محدث.
أما لو تم تحميل تحديثات جديدة، فأعد تشغيل البوت:

sudo systemctl restart botiraq


⸻

🧩 سادساً: إذا أردت تعديل ملف الخدمة نفسه

لفتح ملف الخدمة:

sudo nano /etc/systemd/system/botiraq.service

بعد أي تعديل، نفّذ:

sudo systemctl daemon-reload
sudo systemctl restart botiraq


⸻

🔄 سابعاً: تحديث النظام العام

sudo apt update && sudo apt upgrade -y


⸻

🧠 ثامناً: لو واجهت مشكلة (تشخيص سريع)
	1.	تحقق من الحالة:

sudo systemctl status botiraq


	2.	شوف السجلات:

sudo journalctl -u botiraq -f


	3.	أعد تشغيل البوت:

sudo systemctl restart botiraq



⸻

💾 تاسعاً: نسخة احتياطية للمشروع

احفظ نسخة من مشروعك:

tar -czvf Bot-iraq-backup.tar.gz /home/abdalwahab/Bot-iraq

واسترجعها لاحقًا بـ:

tar -xzvf Bot-iraq-backup.tar.gz


⸻

🧩 عاشراً: تحديث تلقائي (اختياري احترافي)

إذا أردت الراسبيري يسحب آخر تحديث من GitHub تلقائيًا كل يوم،
أنشئ مهمة cron job:

crontab -e

ثم أضف في آخر الملف السطر التالي:

0 3 * * * cd /home/abdalwahab/Bot-iraq && git pull && sudo systemctl restart botiraq

هذا يجعل النظام كل يوم الساعة 3 الفجر يقوم بتحديث المشروع ويعيد تشغيل البوت تلقائيًا 🔁

⸻

هل ترغب أجهز لك ملف نصي .txt فيه كل هذه الأوامر مرتبة ومنسقة
حتى تنسخها بسهولة وتحتفظ بها داخل الراسبيري؟
